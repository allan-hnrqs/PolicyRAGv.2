# Final Blind Acceptance Set

This folder is reserved for the final blind acceptance set.

Rules:

- Do not use this set for active tuning.
- Do not inspect per-case failures while architecture experiments are still in flight.
- Only run this set at milestone reviews after a candidate system has already cleared dev and frozen holdout.
- If the final set exposes a weakness, add fresh sibling cases to dev/holdout rather than iterating directly on the sealed final items.

Current status:

- not authored yet
- should be built after the rebuilt 39-case parity suite is stable
